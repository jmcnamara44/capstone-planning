import constants from './../constants';
const { c } = constants;

export default (state = {}, action) => {

  switch (action.type) {
    case c.FIND_DIRECTIONS:
      let newState = 

  default:
    return state;
  }
}
